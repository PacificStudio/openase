server:
  host: 0.0.0.0
  port: 40023
  read_timeout: 15s
  write_timeout: 15s
  shutdown_timeout: 10s

database:
  dsn: postgres://openase:openase@localhost:5432/openase?sslmode=disable

orchestrator:
  tick_interval: 5s

log:
  level: info
  format: text
