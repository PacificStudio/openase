pre-commit:
  parallel: true
  commands:
    gofmt:
      glob: "*.go"
      run: make fmt-check
    go-build:
      glob: "*.go"
      run: make build
    web-check:
      glob: "web/**/*.{css,html,js,json,svelte,ts}"
      run: make web-check
