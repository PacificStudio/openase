package ticket

// Pickup diagnosis types and ent-backed implementation currently live in
// service.go so the existing openase-228 service-layer Ent exception stays
// centralized in one file until the ticket repo boundary lands.
