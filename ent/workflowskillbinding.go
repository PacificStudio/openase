// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/BetterAndBetterII/openase/ent/skill"
	"github.com/BetterAndBetterII/openase/ent/skillversion"
	"github.com/BetterAndBetterII/openase/ent/workflow"
	"github.com/BetterAndBetterII/openase/ent/workflowskillbinding"
	"github.com/google/uuid"
)

// WorkflowSkillBinding is the model entity for the WorkflowSkillBinding schema.
type WorkflowSkillBinding struct {
	config `json:"-"`
	// ID of the ent.
	ID uuid.UUID `json:"id,omitempty"`
	// WorkflowID holds the value of the "workflow_id" field.
	WorkflowID uuid.UUID `json:"workflow_id,omitempty"`
	// SkillID holds the value of the "skill_id" field.
	SkillID uuid.UUID `json:"skill_id,omitempty"`
	// RequiredVersionID holds the value of the "required_version_id" field.
	RequiredVersionID *uuid.UUID `json:"required_version_id,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the WorkflowSkillBindingQuery when eager-loading is set.
	Edges        WorkflowSkillBindingEdges `json:"edges"`
	selectValues sql.SelectValues
}

// WorkflowSkillBindingEdges holds the relations/edges for other nodes in the graph.
type WorkflowSkillBindingEdges struct {
	// Workflow holds the value of the workflow edge.
	Workflow *Workflow `json:"workflow,omitempty"`
	// Skill holds the value of the skill edge.
	Skill *Skill `json:"skill,omitempty"`
	// RequiredVersion holds the value of the required_version edge.
	RequiredVersion *SkillVersion `json:"required_version,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [3]bool
}

// WorkflowOrErr returns the Workflow value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e WorkflowSkillBindingEdges) WorkflowOrErr() (*Workflow, error) {
	if e.Workflow != nil {
		return e.Workflow, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: workflow.Label}
	}
	return nil, &NotLoadedError{edge: "workflow"}
}

// SkillOrErr returns the Skill value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e WorkflowSkillBindingEdges) SkillOrErr() (*Skill, error) {
	if e.Skill != nil {
		return e.Skill, nil
	} else if e.loadedTypes[1] {
		return nil, &NotFoundError{label: skill.Label}
	}
	return nil, &NotLoadedError{edge: "skill"}
}

// RequiredVersionOrErr returns the RequiredVersion value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e WorkflowSkillBindingEdges) RequiredVersionOrErr() (*SkillVersion, error) {
	if e.RequiredVersion != nil {
		return e.RequiredVersion, nil
	} else if e.loadedTypes[2] {
		return nil, &NotFoundError{label: skillversion.Label}
	}
	return nil, &NotLoadedError{edge: "required_version"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*WorkflowSkillBinding) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case workflowskillbinding.FieldRequiredVersionID:
			values[i] = &sql.NullScanner{S: new(uuid.UUID)}
		case workflowskillbinding.FieldCreatedAt:
			values[i] = new(sql.NullTime)
		case workflowskillbinding.FieldID, workflowskillbinding.FieldWorkflowID, workflowskillbinding.FieldSkillID:
			values[i] = new(uuid.UUID)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the WorkflowSkillBinding fields.
func (_m *WorkflowSkillBinding) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case workflowskillbinding.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				_m.ID = *value
			}
		case workflowskillbinding.FieldWorkflowID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field workflow_id", values[i])
			} else if value != nil {
				_m.WorkflowID = *value
			}
		case workflowskillbinding.FieldSkillID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field skill_id", values[i])
			} else if value != nil {
				_m.SkillID = *value
			}
		case workflowskillbinding.FieldRequiredVersionID:
			if value, ok := values[i].(*sql.NullScanner); !ok {
				return fmt.Errorf("unexpected type %T for field required_version_id", values[i])
			} else if value.Valid {
				_m.RequiredVersionID = new(uuid.UUID)
				*_m.RequiredVersionID = *value.S.(*uuid.UUID)
			}
		case workflowskillbinding.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				_m.CreatedAt = value.Time
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the WorkflowSkillBinding.
// This includes values selected through modifiers, order, etc.
func (_m *WorkflowSkillBinding) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryWorkflow queries the "workflow" edge of the WorkflowSkillBinding entity.
func (_m *WorkflowSkillBinding) QueryWorkflow() *WorkflowQuery {
	return NewWorkflowSkillBindingClient(_m.config).QueryWorkflow(_m)
}

// QuerySkill queries the "skill" edge of the WorkflowSkillBinding entity.
func (_m *WorkflowSkillBinding) QuerySkill() *SkillQuery {
	return NewWorkflowSkillBindingClient(_m.config).QuerySkill(_m)
}

// QueryRequiredVersion queries the "required_version" edge of the WorkflowSkillBinding entity.
func (_m *WorkflowSkillBinding) QueryRequiredVersion() *SkillVersionQuery {
	return NewWorkflowSkillBindingClient(_m.config).QueryRequiredVersion(_m)
}

// Update returns a builder for updating this WorkflowSkillBinding.
// Note that you need to call WorkflowSkillBinding.Unwrap() before calling this method if this WorkflowSkillBinding
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *WorkflowSkillBinding) Update() *WorkflowSkillBindingUpdateOne {
	return NewWorkflowSkillBindingClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the WorkflowSkillBinding entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *WorkflowSkillBinding) Unwrap() *WorkflowSkillBinding {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: WorkflowSkillBinding is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *WorkflowSkillBinding) String() string {
	var builder strings.Builder
	builder.WriteString("WorkflowSkillBinding(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("workflow_id=")
	builder.WriteString(fmt.Sprintf("%v", _m.WorkflowID))
	builder.WriteString(", ")
	builder.WriteString("skill_id=")
	builder.WriteString(fmt.Sprintf("%v", _m.SkillID))
	builder.WriteString(", ")
	if v := _m.RequiredVersionID; v != nil {
		builder.WriteString("required_version_id=")
		builder.WriteString(fmt.Sprintf("%v", *v))
	}
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(_m.CreatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// WorkflowSkillBindings is a parsable slice of WorkflowSkillBinding.
type WorkflowSkillBindings []*WorkflowSkillBinding
