// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/BetterAndBetterII/openase/ent/agentrun"
	"github.com/BetterAndBetterII/openase/ent/projectrepo"
	"github.com/BetterAndBetterII/openase/ent/ticket"
	"github.com/BetterAndBetterII/openase/ent/ticketrepoworkspace"
	"github.com/google/uuid"
)

// TicketRepoWorkspace is the model entity for the TicketRepoWorkspace schema.
type TicketRepoWorkspace struct {
	config `json:"-"`
	// ID of the ent.
	ID uuid.UUID `json:"id,omitempty"`
	// TicketID holds the value of the "ticket_id" field.
	TicketID uuid.UUID `json:"ticket_id,omitempty"`
	// AgentRunID holds the value of the "agent_run_id" field.
	AgentRunID uuid.UUID `json:"agent_run_id,omitempty"`
	// RepoID holds the value of the "repo_id" field.
	RepoID uuid.UUID `json:"repo_id,omitempty"`
	// WorkspaceRoot holds the value of the "workspace_root" field.
	WorkspaceRoot string `json:"workspace_root,omitempty"`
	// RepoPath holds the value of the "repo_path" field.
	RepoPath string `json:"repo_path,omitempty"`
	// BranchName holds the value of the "branch_name" field.
	BranchName string `json:"branch_name,omitempty"`
	// State holds the value of the "state" field.
	State ticketrepoworkspace.State `json:"state,omitempty"`
	// HeadCommit holds the value of the "head_commit" field.
	HeadCommit string `json:"head_commit,omitempty"`
	// LastError holds the value of the "last_error" field.
	LastError string `json:"last_error,omitempty"`
	// PreparedAt holds the value of the "prepared_at" field.
	PreparedAt *time.Time `json:"prepared_at,omitempty"`
	// CleanedAt holds the value of the "cleaned_at" field.
	CleanedAt *time.Time `json:"cleaned_at,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// UpdatedAt holds the value of the "updated_at" field.
	UpdatedAt time.Time `json:"updated_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the TicketRepoWorkspaceQuery when eager-loading is set.
	Edges        TicketRepoWorkspaceEdges `json:"edges"`
	selectValues sql.SelectValues
}

// TicketRepoWorkspaceEdges holds the relations/edges for other nodes in the graph.
type TicketRepoWorkspaceEdges struct {
	// Ticket holds the value of the ticket edge.
	Ticket *Ticket `json:"ticket,omitempty"`
	// AgentRun holds the value of the agent_run edge.
	AgentRun *AgentRun `json:"agent_run,omitempty"`
	// Repo holds the value of the repo edge.
	Repo *ProjectRepo `json:"repo,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [3]bool
}

// TicketOrErr returns the Ticket value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e TicketRepoWorkspaceEdges) TicketOrErr() (*Ticket, error) {
	if e.Ticket != nil {
		return e.Ticket, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: ticket.Label}
	}
	return nil, &NotLoadedError{edge: "ticket"}
}

// AgentRunOrErr returns the AgentRun value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e TicketRepoWorkspaceEdges) AgentRunOrErr() (*AgentRun, error) {
	if e.AgentRun != nil {
		return e.AgentRun, nil
	} else if e.loadedTypes[1] {
		return nil, &NotFoundError{label: agentrun.Label}
	}
	return nil, &NotLoadedError{edge: "agent_run"}
}

// RepoOrErr returns the Repo value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e TicketRepoWorkspaceEdges) RepoOrErr() (*ProjectRepo, error) {
	if e.Repo != nil {
		return e.Repo, nil
	} else if e.loadedTypes[2] {
		return nil, &NotFoundError{label: projectrepo.Label}
	}
	return nil, &NotLoadedError{edge: "repo"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*TicketRepoWorkspace) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case ticketrepoworkspace.FieldWorkspaceRoot, ticketrepoworkspace.FieldRepoPath, ticketrepoworkspace.FieldBranchName, ticketrepoworkspace.FieldState, ticketrepoworkspace.FieldHeadCommit, ticketrepoworkspace.FieldLastError:
			values[i] = new(sql.NullString)
		case ticketrepoworkspace.FieldPreparedAt, ticketrepoworkspace.FieldCleanedAt, ticketrepoworkspace.FieldCreatedAt, ticketrepoworkspace.FieldUpdatedAt:
			values[i] = new(sql.NullTime)
		case ticketrepoworkspace.FieldID, ticketrepoworkspace.FieldTicketID, ticketrepoworkspace.FieldAgentRunID, ticketrepoworkspace.FieldRepoID:
			values[i] = new(uuid.UUID)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the TicketRepoWorkspace fields.
func (_m *TicketRepoWorkspace) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case ticketrepoworkspace.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				_m.ID = *value
			}
		case ticketrepoworkspace.FieldTicketID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field ticket_id", values[i])
			} else if value != nil {
				_m.TicketID = *value
			}
		case ticketrepoworkspace.FieldAgentRunID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field agent_run_id", values[i])
			} else if value != nil {
				_m.AgentRunID = *value
			}
		case ticketrepoworkspace.FieldRepoID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field repo_id", values[i])
			} else if value != nil {
				_m.RepoID = *value
			}
		case ticketrepoworkspace.FieldWorkspaceRoot:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field workspace_root", values[i])
			} else if value.Valid {
				_m.WorkspaceRoot = value.String
			}
		case ticketrepoworkspace.FieldRepoPath:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field repo_path", values[i])
			} else if value.Valid {
				_m.RepoPath = value.String
			}
		case ticketrepoworkspace.FieldBranchName:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field branch_name", values[i])
			} else if value.Valid {
				_m.BranchName = value.String
			}
		case ticketrepoworkspace.FieldState:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field state", values[i])
			} else if value.Valid {
				_m.State = ticketrepoworkspace.State(value.String)
			}
		case ticketrepoworkspace.FieldHeadCommit:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field head_commit", values[i])
			} else if value.Valid {
				_m.HeadCommit = value.String
			}
		case ticketrepoworkspace.FieldLastError:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field last_error", values[i])
			} else if value.Valid {
				_m.LastError = value.String
			}
		case ticketrepoworkspace.FieldPreparedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field prepared_at", values[i])
			} else if value.Valid {
				_m.PreparedAt = new(time.Time)
				*_m.PreparedAt = value.Time
			}
		case ticketrepoworkspace.FieldCleanedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field cleaned_at", values[i])
			} else if value.Valid {
				_m.CleanedAt = new(time.Time)
				*_m.CleanedAt = value.Time
			}
		case ticketrepoworkspace.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				_m.CreatedAt = value.Time
			}
		case ticketrepoworkspace.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				_m.UpdatedAt = value.Time
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the TicketRepoWorkspace.
// This includes values selected through modifiers, order, etc.
func (_m *TicketRepoWorkspace) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryTicket queries the "ticket" edge of the TicketRepoWorkspace entity.
func (_m *TicketRepoWorkspace) QueryTicket() *TicketQuery {
	return NewTicketRepoWorkspaceClient(_m.config).QueryTicket(_m)
}

// QueryAgentRun queries the "agent_run" edge of the TicketRepoWorkspace entity.
func (_m *TicketRepoWorkspace) QueryAgentRun() *AgentRunQuery {
	return NewTicketRepoWorkspaceClient(_m.config).QueryAgentRun(_m)
}

// QueryRepo queries the "repo" edge of the TicketRepoWorkspace entity.
func (_m *TicketRepoWorkspace) QueryRepo() *ProjectRepoQuery {
	return NewTicketRepoWorkspaceClient(_m.config).QueryRepo(_m)
}

// Update returns a builder for updating this TicketRepoWorkspace.
// Note that you need to call TicketRepoWorkspace.Unwrap() before calling this method if this TicketRepoWorkspace
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *TicketRepoWorkspace) Update() *TicketRepoWorkspaceUpdateOne {
	return NewTicketRepoWorkspaceClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the TicketRepoWorkspace entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *TicketRepoWorkspace) Unwrap() *TicketRepoWorkspace {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: TicketRepoWorkspace is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *TicketRepoWorkspace) String() string {
	var builder strings.Builder
	builder.WriteString("TicketRepoWorkspace(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("ticket_id=")
	builder.WriteString(fmt.Sprintf("%v", _m.TicketID))
	builder.WriteString(", ")
	builder.WriteString("agent_run_id=")
	builder.WriteString(fmt.Sprintf("%v", _m.AgentRunID))
	builder.WriteString(", ")
	builder.WriteString("repo_id=")
	builder.WriteString(fmt.Sprintf("%v", _m.RepoID))
	builder.WriteString(", ")
	builder.WriteString("workspace_root=")
	builder.WriteString(_m.WorkspaceRoot)
	builder.WriteString(", ")
	builder.WriteString("repo_path=")
	builder.WriteString(_m.RepoPath)
	builder.WriteString(", ")
	builder.WriteString("branch_name=")
	builder.WriteString(_m.BranchName)
	builder.WriteString(", ")
	builder.WriteString("state=")
	builder.WriteString(fmt.Sprintf("%v", _m.State))
	builder.WriteString(", ")
	builder.WriteString("head_commit=")
	builder.WriteString(_m.HeadCommit)
	builder.WriteString(", ")
	builder.WriteString("last_error=")
	builder.WriteString(_m.LastError)
	builder.WriteString(", ")
	if v := _m.PreparedAt; v != nil {
		builder.WriteString("prepared_at=")
		builder.WriteString(v.Format(time.ANSIC))
	}
	builder.WriteString(", ")
	if v := _m.CleanedAt; v != nil {
		builder.WriteString("cleaned_at=")
		builder.WriteString(v.Format(time.ANSIC))
	}
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(_m.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("updated_at=")
	builder.WriteString(_m.UpdatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// TicketRepoWorkspaces is a parsable slice of TicketRepoWorkspace.
type TicketRepoWorkspaces []*TicketRepoWorkspace
