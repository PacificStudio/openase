// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/BetterAndBetterII/openase/ent/projectrepo"
	"github.com/BetterAndBetterII/openase/ent/ticket"
	"github.com/BetterAndBetterII/openase/ent/ticketreposcope"
	"github.com/google/uuid"
)

// TicketRepoScope is the model entity for the TicketRepoScope schema.
type TicketRepoScope struct {
	config `json:"-"`
	// ID of the ent.
	ID uuid.UUID `json:"id,omitempty"`
	// TicketID holds the value of the "ticket_id" field.
	TicketID uuid.UUID `json:"ticket_id,omitempty"`
	// RepoID holds the value of the "repo_id" field.
	RepoID uuid.UUID `json:"repo_id,omitempty"`
	// BranchName holds the value of the "branch_name" field.
	BranchName string `json:"branch_name,omitempty"`
	// PullRequestURL holds the value of the "pull_request_url" field.
	PullRequestURL string `json:"pull_request_url,omitempty"`
	// PrStatus holds the value of the "pr_status" field.
	PrStatus ticketreposcope.PrStatus `json:"pr_status,omitempty"`
	// CiStatus holds the value of the "ci_status" field.
	CiStatus ticketreposcope.CiStatus `json:"ci_status,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the TicketRepoScopeQuery when eager-loading is set.
	Edges        TicketRepoScopeEdges `json:"edges"`
	selectValues sql.SelectValues
}

// TicketRepoScopeEdges holds the relations/edges for other nodes in the graph.
type TicketRepoScopeEdges struct {
	// Ticket holds the value of the ticket edge.
	Ticket *Ticket `json:"ticket,omitempty"`
	// Repo holds the value of the repo edge.
	Repo *ProjectRepo `json:"repo,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [2]bool
}

// TicketOrErr returns the Ticket value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e TicketRepoScopeEdges) TicketOrErr() (*Ticket, error) {
	if e.Ticket != nil {
		return e.Ticket, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: ticket.Label}
	}
	return nil, &NotLoadedError{edge: "ticket"}
}

// RepoOrErr returns the Repo value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e TicketRepoScopeEdges) RepoOrErr() (*ProjectRepo, error) {
	if e.Repo != nil {
		return e.Repo, nil
	} else if e.loadedTypes[1] {
		return nil, &NotFoundError{label: projectrepo.Label}
	}
	return nil, &NotLoadedError{edge: "repo"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*TicketRepoScope) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case ticketreposcope.FieldBranchName, ticketreposcope.FieldPullRequestURL, ticketreposcope.FieldPrStatus, ticketreposcope.FieldCiStatus:
			values[i] = new(sql.NullString)
		case ticketreposcope.FieldID, ticketreposcope.FieldTicketID, ticketreposcope.FieldRepoID:
			values[i] = new(uuid.UUID)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the TicketRepoScope fields.
func (_m *TicketRepoScope) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case ticketreposcope.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				_m.ID = *value
			}
		case ticketreposcope.FieldTicketID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field ticket_id", values[i])
			} else if value != nil {
				_m.TicketID = *value
			}
		case ticketreposcope.FieldRepoID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field repo_id", values[i])
			} else if value != nil {
				_m.RepoID = *value
			}
		case ticketreposcope.FieldBranchName:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field branch_name", values[i])
			} else if value.Valid {
				_m.BranchName = value.String
			}
		case ticketreposcope.FieldPullRequestURL:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field pull_request_url", values[i])
			} else if value.Valid {
				_m.PullRequestURL = value.String
			}
		case ticketreposcope.FieldPrStatus:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field pr_status", values[i])
			} else if value.Valid {
				_m.PrStatus = ticketreposcope.PrStatus(value.String)
			}
		case ticketreposcope.FieldCiStatus:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field ci_status", values[i])
			} else if value.Valid {
				_m.CiStatus = ticketreposcope.CiStatus(value.String)
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the TicketRepoScope.
// This includes values selected through modifiers, order, etc.
func (_m *TicketRepoScope) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryTicket queries the "ticket" edge of the TicketRepoScope entity.
func (_m *TicketRepoScope) QueryTicket() *TicketQuery {
	return NewTicketRepoScopeClient(_m.config).QueryTicket(_m)
}

// QueryRepo queries the "repo" edge of the TicketRepoScope entity.
func (_m *TicketRepoScope) QueryRepo() *ProjectRepoQuery {
	return NewTicketRepoScopeClient(_m.config).QueryRepo(_m)
}

// Update returns a builder for updating this TicketRepoScope.
// Note that you need to call TicketRepoScope.Unwrap() before calling this method if this TicketRepoScope
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *TicketRepoScope) Update() *TicketRepoScopeUpdateOne {
	return NewTicketRepoScopeClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the TicketRepoScope entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *TicketRepoScope) Unwrap() *TicketRepoScope {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: TicketRepoScope is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *TicketRepoScope) String() string {
	var builder strings.Builder
	builder.WriteString("TicketRepoScope(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("ticket_id=")
	builder.WriteString(fmt.Sprintf("%v", _m.TicketID))
	builder.WriteString(", ")
	builder.WriteString("repo_id=")
	builder.WriteString(fmt.Sprintf("%v", _m.RepoID))
	builder.WriteString(", ")
	builder.WriteString("branch_name=")
	builder.WriteString(_m.BranchName)
	builder.WriteString(", ")
	builder.WriteString("pull_request_url=")
	builder.WriteString(_m.PullRequestURL)
	builder.WriteString(", ")
	builder.WriteString("pr_status=")
	builder.WriteString(fmt.Sprintf("%v", _m.PrStatus))
	builder.WriteString(", ")
	builder.WriteString("ci_status=")
	builder.WriteString(fmt.Sprintf("%v", _m.CiStatus))
	builder.WriteByte(')')
	return builder.String()
}

// TicketRepoScopes is a parsable slice of TicketRepoScope.
type TicketRepoScopes []*TicketRepoScope
