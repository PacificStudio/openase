// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/BetterAndBetterII/openase/ent/predicate"
	"github.com/BetterAndBetterII/openase/ent/skill"
	"github.com/BetterAndBetterII/openase/ent/skillversion"
	"github.com/BetterAndBetterII/openase/ent/workflow"
	"github.com/BetterAndBetterII/openase/ent/workflowskillbinding"
	"github.com/google/uuid"
)

// WorkflowSkillBindingQuery is the builder for querying WorkflowSkillBinding entities.
type WorkflowSkillBindingQuery struct {
	config
	ctx                 *QueryContext
	order               []workflowskillbinding.OrderOption
	inters              []Interceptor
	predicates          []predicate.WorkflowSkillBinding
	withWorkflow        *WorkflowQuery
	withSkill           *SkillQuery
	withRequiredVersion *SkillVersionQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the WorkflowSkillBindingQuery builder.
func (_q *WorkflowSkillBindingQuery) Where(ps ...predicate.WorkflowSkillBinding) *WorkflowSkillBindingQuery {
	_q.predicates = append(_q.predicates, ps...)
	return _q
}

// Limit the number of records to be returned by this query.
func (_q *WorkflowSkillBindingQuery) Limit(limit int) *WorkflowSkillBindingQuery {
	_q.ctx.Limit = &limit
	return _q
}

// Offset to start from.
func (_q *WorkflowSkillBindingQuery) Offset(offset int) *WorkflowSkillBindingQuery {
	_q.ctx.Offset = &offset
	return _q
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (_q *WorkflowSkillBindingQuery) Unique(unique bool) *WorkflowSkillBindingQuery {
	_q.ctx.Unique = &unique
	return _q
}

// Order specifies how the records should be ordered.
func (_q *WorkflowSkillBindingQuery) Order(o ...workflowskillbinding.OrderOption) *WorkflowSkillBindingQuery {
	_q.order = append(_q.order, o...)
	return _q
}

// QueryWorkflow chains the current query on the "workflow" edge.
func (_q *WorkflowSkillBindingQuery) QueryWorkflow() *WorkflowQuery {
	query := (&WorkflowClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(workflowskillbinding.Table, workflowskillbinding.FieldID, selector),
			sqlgraph.To(workflow.Table, workflow.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, workflowskillbinding.WorkflowTable, workflowskillbinding.WorkflowColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QuerySkill chains the current query on the "skill" edge.
func (_q *WorkflowSkillBindingQuery) QuerySkill() *SkillQuery {
	query := (&SkillClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(workflowskillbinding.Table, workflowskillbinding.FieldID, selector),
			sqlgraph.To(skill.Table, skill.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, workflowskillbinding.SkillTable, workflowskillbinding.SkillColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryRequiredVersion chains the current query on the "required_version" edge.
func (_q *WorkflowSkillBindingQuery) QueryRequiredVersion() *SkillVersionQuery {
	query := (&SkillVersionClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(workflowskillbinding.Table, workflowskillbinding.FieldID, selector),
			sqlgraph.To(skillversion.Table, skillversion.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, workflowskillbinding.RequiredVersionTable, workflowskillbinding.RequiredVersionColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first WorkflowSkillBinding entity from the query.
// Returns a *NotFoundError when no WorkflowSkillBinding was found.
func (_q *WorkflowSkillBindingQuery) First(ctx context.Context) (*WorkflowSkillBinding, error) {
	nodes, err := _q.Limit(1).All(setContextOp(ctx, _q.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{workflowskillbinding.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (_q *WorkflowSkillBindingQuery) FirstX(ctx context.Context) *WorkflowSkillBinding {
	node, err := _q.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first WorkflowSkillBinding ID from the query.
// Returns a *NotFoundError when no WorkflowSkillBinding ID was found.
func (_q *WorkflowSkillBindingQuery) FirstID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = _q.Limit(1).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{workflowskillbinding.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (_q *WorkflowSkillBindingQuery) FirstIDX(ctx context.Context) uuid.UUID {
	id, err := _q.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single WorkflowSkillBinding entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one WorkflowSkillBinding entity is found.
// Returns a *NotFoundError when no WorkflowSkillBinding entities are found.
func (_q *WorkflowSkillBindingQuery) Only(ctx context.Context) (*WorkflowSkillBinding, error) {
	nodes, err := _q.Limit(2).All(setContextOp(ctx, _q.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{workflowskillbinding.Label}
	default:
		return nil, &NotSingularError{workflowskillbinding.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (_q *WorkflowSkillBindingQuery) OnlyX(ctx context.Context) *WorkflowSkillBinding {
	node, err := _q.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only WorkflowSkillBinding ID in the query.
// Returns a *NotSingularError when more than one WorkflowSkillBinding ID is found.
// Returns a *NotFoundError when no entities are found.
func (_q *WorkflowSkillBindingQuery) OnlyID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = _q.Limit(2).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{workflowskillbinding.Label}
	default:
		err = &NotSingularError{workflowskillbinding.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (_q *WorkflowSkillBindingQuery) OnlyIDX(ctx context.Context) uuid.UUID {
	id, err := _q.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of WorkflowSkillBindings.
func (_q *WorkflowSkillBindingQuery) All(ctx context.Context) ([]*WorkflowSkillBinding, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryAll)
	if err := _q.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*WorkflowSkillBinding, *WorkflowSkillBindingQuery]()
	return withInterceptors[[]*WorkflowSkillBinding](ctx, _q, qr, _q.inters)
}

// AllX is like All, but panics if an error occurs.
func (_q *WorkflowSkillBindingQuery) AllX(ctx context.Context) []*WorkflowSkillBinding {
	nodes, err := _q.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of WorkflowSkillBinding IDs.
func (_q *WorkflowSkillBindingQuery) IDs(ctx context.Context) (ids []uuid.UUID, err error) {
	if _q.ctx.Unique == nil && _q.path != nil {
		_q.Unique(true)
	}
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryIDs)
	if err = _q.Select(workflowskillbinding.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (_q *WorkflowSkillBindingQuery) IDsX(ctx context.Context) []uuid.UUID {
	ids, err := _q.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (_q *WorkflowSkillBindingQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryCount)
	if err := _q.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, _q, querierCount[*WorkflowSkillBindingQuery](), _q.inters)
}

// CountX is like Count, but panics if an error occurs.
func (_q *WorkflowSkillBindingQuery) CountX(ctx context.Context) int {
	count, err := _q.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (_q *WorkflowSkillBindingQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryExist)
	switch _, err := _q.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (_q *WorkflowSkillBindingQuery) ExistX(ctx context.Context) bool {
	exist, err := _q.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the WorkflowSkillBindingQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (_q *WorkflowSkillBindingQuery) Clone() *WorkflowSkillBindingQuery {
	if _q == nil {
		return nil
	}
	return &WorkflowSkillBindingQuery{
		config:              _q.config,
		ctx:                 _q.ctx.Clone(),
		order:               append([]workflowskillbinding.OrderOption{}, _q.order...),
		inters:              append([]Interceptor{}, _q.inters...),
		predicates:          append([]predicate.WorkflowSkillBinding{}, _q.predicates...),
		withWorkflow:        _q.withWorkflow.Clone(),
		withSkill:           _q.withSkill.Clone(),
		withRequiredVersion: _q.withRequiredVersion.Clone(),
		// clone intermediate query.
		sql:  _q.sql.Clone(),
		path: _q.path,
	}
}

// WithWorkflow tells the query-builder to eager-load the nodes that are connected to
// the "workflow" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *WorkflowSkillBindingQuery) WithWorkflow(opts ...func(*WorkflowQuery)) *WorkflowSkillBindingQuery {
	query := (&WorkflowClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withWorkflow = query
	return _q
}

// WithSkill tells the query-builder to eager-load the nodes that are connected to
// the "skill" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *WorkflowSkillBindingQuery) WithSkill(opts ...func(*SkillQuery)) *WorkflowSkillBindingQuery {
	query := (&SkillClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withSkill = query
	return _q
}

// WithRequiredVersion tells the query-builder to eager-load the nodes that are connected to
// the "required_version" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *WorkflowSkillBindingQuery) WithRequiredVersion(opts ...func(*SkillVersionQuery)) *WorkflowSkillBindingQuery {
	query := (&SkillVersionClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withRequiredVersion = query
	return _q
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		WorkflowID uuid.UUID `json:"workflow_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.WorkflowSkillBinding.Query().
//		GroupBy(workflowskillbinding.FieldWorkflowID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (_q *WorkflowSkillBindingQuery) GroupBy(field string, fields ...string) *WorkflowSkillBindingGroupBy {
	_q.ctx.Fields = append([]string{field}, fields...)
	grbuild := &WorkflowSkillBindingGroupBy{build: _q}
	grbuild.flds = &_q.ctx.Fields
	grbuild.label = workflowskillbinding.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		WorkflowID uuid.UUID `json:"workflow_id,omitempty"`
//	}
//
//	client.WorkflowSkillBinding.Query().
//		Select(workflowskillbinding.FieldWorkflowID).
//		Scan(ctx, &v)
func (_q *WorkflowSkillBindingQuery) Select(fields ...string) *WorkflowSkillBindingSelect {
	_q.ctx.Fields = append(_q.ctx.Fields, fields...)
	sbuild := &WorkflowSkillBindingSelect{WorkflowSkillBindingQuery: _q}
	sbuild.label = workflowskillbinding.Label
	sbuild.flds, sbuild.scan = &_q.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a WorkflowSkillBindingSelect configured with the given aggregations.
func (_q *WorkflowSkillBindingQuery) Aggregate(fns ...AggregateFunc) *WorkflowSkillBindingSelect {
	return _q.Select().Aggregate(fns...)
}

func (_q *WorkflowSkillBindingQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range _q.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, _q); err != nil {
				return err
			}
		}
	}
	for _, f := range _q.ctx.Fields {
		if !workflowskillbinding.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if _q.path != nil {
		prev, err := _q.path(ctx)
		if err != nil {
			return err
		}
		_q.sql = prev
	}
	return nil
}

func (_q *WorkflowSkillBindingQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*WorkflowSkillBinding, error) {
	var (
		nodes       = []*WorkflowSkillBinding{}
		_spec       = _q.querySpec()
		loadedTypes = [3]bool{
			_q.withWorkflow != nil,
			_q.withSkill != nil,
			_q.withRequiredVersion != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*WorkflowSkillBinding).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &WorkflowSkillBinding{config: _q.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, _q.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := _q.withWorkflow; query != nil {
		if err := _q.loadWorkflow(ctx, query, nodes, nil,
			func(n *WorkflowSkillBinding, e *Workflow) { n.Edges.Workflow = e }); err != nil {
			return nil, err
		}
	}
	if query := _q.withSkill; query != nil {
		if err := _q.loadSkill(ctx, query, nodes, nil,
			func(n *WorkflowSkillBinding, e *Skill) { n.Edges.Skill = e }); err != nil {
			return nil, err
		}
	}
	if query := _q.withRequiredVersion; query != nil {
		if err := _q.loadRequiredVersion(ctx, query, nodes, nil,
			func(n *WorkflowSkillBinding, e *SkillVersion) { n.Edges.RequiredVersion = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (_q *WorkflowSkillBindingQuery) loadWorkflow(ctx context.Context, query *WorkflowQuery, nodes []*WorkflowSkillBinding, init func(*WorkflowSkillBinding), assign func(*WorkflowSkillBinding, *Workflow)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*WorkflowSkillBinding)
	for i := range nodes {
		fk := nodes[i].WorkflowID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(workflow.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "workflow_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (_q *WorkflowSkillBindingQuery) loadSkill(ctx context.Context, query *SkillQuery, nodes []*WorkflowSkillBinding, init func(*WorkflowSkillBinding), assign func(*WorkflowSkillBinding, *Skill)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*WorkflowSkillBinding)
	for i := range nodes {
		fk := nodes[i].SkillID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(skill.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "skill_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (_q *WorkflowSkillBindingQuery) loadRequiredVersion(ctx context.Context, query *SkillVersionQuery, nodes []*WorkflowSkillBinding, init func(*WorkflowSkillBinding), assign func(*WorkflowSkillBinding, *SkillVersion)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*WorkflowSkillBinding)
	for i := range nodes {
		if nodes[i].RequiredVersionID == nil {
			continue
		}
		fk := *nodes[i].RequiredVersionID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(skillversion.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "required_version_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (_q *WorkflowSkillBindingQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := _q.querySpec()
	_spec.Node.Columns = _q.ctx.Fields
	if len(_q.ctx.Fields) > 0 {
		_spec.Unique = _q.ctx.Unique != nil && *_q.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, _q.driver, _spec)
}

func (_q *WorkflowSkillBindingQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(workflowskillbinding.Table, workflowskillbinding.Columns, sqlgraph.NewFieldSpec(workflowskillbinding.FieldID, field.TypeUUID))
	_spec.From = _q.sql
	if unique := _q.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if _q.path != nil {
		_spec.Unique = true
	}
	if fields := _q.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, workflowskillbinding.FieldID)
		for i := range fields {
			if fields[i] != workflowskillbinding.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if _q.withWorkflow != nil {
			_spec.Node.AddColumnOnce(workflowskillbinding.FieldWorkflowID)
		}
		if _q.withSkill != nil {
			_spec.Node.AddColumnOnce(workflowskillbinding.FieldSkillID)
		}
		if _q.withRequiredVersion != nil {
			_spec.Node.AddColumnOnce(workflowskillbinding.FieldRequiredVersionID)
		}
	}
	if ps := _q.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := _q.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := _q.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := _q.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (_q *WorkflowSkillBindingQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(_q.driver.Dialect())
	t1 := builder.Table(workflowskillbinding.Table)
	columns := _q.ctx.Fields
	if len(columns) == 0 {
		columns = workflowskillbinding.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if _q.sql != nil {
		selector = _q.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if _q.ctx.Unique != nil && *_q.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range _q.predicates {
		p(selector)
	}
	for _, p := range _q.order {
		p(selector)
	}
	if offset := _q.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := _q.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// WorkflowSkillBindingGroupBy is the group-by builder for WorkflowSkillBinding entities.
type WorkflowSkillBindingGroupBy struct {
	selector
	build *WorkflowSkillBindingQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (_g *WorkflowSkillBindingGroupBy) Aggregate(fns ...AggregateFunc) *WorkflowSkillBindingGroupBy {
	_g.fns = append(_g.fns, fns...)
	return _g
}

// Scan applies the selector query and scans the result into the given value.
func (_g *WorkflowSkillBindingGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _g.build.ctx, ent.OpQueryGroupBy)
	if err := _g.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*WorkflowSkillBindingQuery, *WorkflowSkillBindingGroupBy](ctx, _g.build, _g, _g.build.inters, v)
}

func (_g *WorkflowSkillBindingGroupBy) sqlScan(ctx context.Context, root *WorkflowSkillBindingQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(_g.fns))
	for _, fn := range _g.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*_g.flds)+len(_g.fns))
		for _, f := range *_g.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*_g.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _g.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// WorkflowSkillBindingSelect is the builder for selecting fields of WorkflowSkillBinding entities.
type WorkflowSkillBindingSelect struct {
	*WorkflowSkillBindingQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (_s *WorkflowSkillBindingSelect) Aggregate(fns ...AggregateFunc) *WorkflowSkillBindingSelect {
	_s.fns = append(_s.fns, fns...)
	return _s
}

// Scan applies the selector query and scans the result into the given value.
func (_s *WorkflowSkillBindingSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _s.ctx, ent.OpQuerySelect)
	if err := _s.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*WorkflowSkillBindingQuery, *WorkflowSkillBindingSelect](ctx, _s.WorkflowSkillBindingQuery, _s, _s.inters, v)
}

func (_s *WorkflowSkillBindingSelect) sqlScan(ctx context.Context, root *WorkflowSkillBindingQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(_s.fns))
	for _, fn := range _s.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*_s.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _s.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
