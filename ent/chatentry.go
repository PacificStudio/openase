// Code generated by ent, DO NOT EDIT.

package ent

import (
	"encoding/json"
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/BetterAndBetterII/openase/ent/chatconversation"
	"github.com/BetterAndBetterII/openase/ent/chatentry"
	"github.com/BetterAndBetterII/openase/ent/chatturn"
	"github.com/google/uuid"
)

// ChatEntry is the model entity for the ChatEntry schema.
type ChatEntry struct {
	config `json:"-"`
	// ID of the ent.
	ID uuid.UUID `json:"id,omitempty"`
	// ConversationID holds the value of the "conversation_id" field.
	ConversationID uuid.UUID `json:"conversation_id,omitempty"`
	// TurnID holds the value of the "turn_id" field.
	TurnID *uuid.UUID `json:"turn_id,omitempty"`
	// Seq holds the value of the "seq" field.
	Seq int `json:"seq,omitempty"`
	// Kind holds the value of the "kind" field.
	Kind string `json:"kind,omitempty"`
	// PayloadJSON holds the value of the "payload_json" field.
	PayloadJSON map[string]interface{} `json:"payload_json,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the ChatEntryQuery when eager-loading is set.
	Edges        ChatEntryEdges `json:"edges"`
	selectValues sql.SelectValues
}

// ChatEntryEdges holds the relations/edges for other nodes in the graph.
type ChatEntryEdges struct {
	// Conversation holds the value of the conversation edge.
	Conversation *ChatConversation `json:"conversation,omitempty"`
	// Turn holds the value of the turn edge.
	Turn *ChatTurn `json:"turn,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [2]bool
}

// ConversationOrErr returns the Conversation value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e ChatEntryEdges) ConversationOrErr() (*ChatConversation, error) {
	if e.Conversation != nil {
		return e.Conversation, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: chatconversation.Label}
	}
	return nil, &NotLoadedError{edge: "conversation"}
}

// TurnOrErr returns the Turn value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e ChatEntryEdges) TurnOrErr() (*ChatTurn, error) {
	if e.Turn != nil {
		return e.Turn, nil
	} else if e.loadedTypes[1] {
		return nil, &NotFoundError{label: chatturn.Label}
	}
	return nil, &NotLoadedError{edge: "turn"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*ChatEntry) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case chatentry.FieldTurnID:
			values[i] = &sql.NullScanner{S: new(uuid.UUID)}
		case chatentry.FieldPayloadJSON:
			values[i] = new([]byte)
		case chatentry.FieldSeq:
			values[i] = new(sql.NullInt64)
		case chatentry.FieldKind:
			values[i] = new(sql.NullString)
		case chatentry.FieldCreatedAt:
			values[i] = new(sql.NullTime)
		case chatentry.FieldID, chatentry.FieldConversationID:
			values[i] = new(uuid.UUID)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the ChatEntry fields.
func (_m *ChatEntry) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case chatentry.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				_m.ID = *value
			}
		case chatentry.FieldConversationID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field conversation_id", values[i])
			} else if value != nil {
				_m.ConversationID = *value
			}
		case chatentry.FieldTurnID:
			if value, ok := values[i].(*sql.NullScanner); !ok {
				return fmt.Errorf("unexpected type %T for field turn_id", values[i])
			} else if value.Valid {
				_m.TurnID = new(uuid.UUID)
				*_m.TurnID = *value.S.(*uuid.UUID)
			}
		case chatentry.FieldSeq:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field seq", values[i])
			} else if value.Valid {
				_m.Seq = int(value.Int64)
			}
		case chatentry.FieldKind:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field kind", values[i])
			} else if value.Valid {
				_m.Kind = value.String
			}
		case chatentry.FieldPayloadJSON:
			if value, ok := values[i].(*[]byte); !ok {
				return fmt.Errorf("unexpected type %T for field payload_json", values[i])
			} else if value != nil && len(*value) > 0 {
				if err := json.Unmarshal(*value, &_m.PayloadJSON); err != nil {
					return fmt.Errorf("unmarshal field payload_json: %w", err)
				}
			}
		case chatentry.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				_m.CreatedAt = value.Time
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the ChatEntry.
// This includes values selected through modifiers, order, etc.
func (_m *ChatEntry) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryConversation queries the "conversation" edge of the ChatEntry entity.
func (_m *ChatEntry) QueryConversation() *ChatConversationQuery {
	return NewChatEntryClient(_m.config).QueryConversation(_m)
}

// QueryTurn queries the "turn" edge of the ChatEntry entity.
func (_m *ChatEntry) QueryTurn() *ChatTurnQuery {
	return NewChatEntryClient(_m.config).QueryTurn(_m)
}

// Update returns a builder for updating this ChatEntry.
// Note that you need to call ChatEntry.Unwrap() before calling this method if this ChatEntry
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *ChatEntry) Update() *ChatEntryUpdateOne {
	return NewChatEntryClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the ChatEntry entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *ChatEntry) Unwrap() *ChatEntry {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: ChatEntry is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *ChatEntry) String() string {
	var builder strings.Builder
	builder.WriteString("ChatEntry(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("conversation_id=")
	builder.WriteString(fmt.Sprintf("%v", _m.ConversationID))
	builder.WriteString(", ")
	if v := _m.TurnID; v != nil {
		builder.WriteString("turn_id=")
		builder.WriteString(fmt.Sprintf("%v", *v))
	}
	builder.WriteString(", ")
	builder.WriteString("seq=")
	builder.WriteString(fmt.Sprintf("%v", _m.Seq))
	builder.WriteString(", ")
	builder.WriteString("kind=")
	builder.WriteString(_m.Kind)
	builder.WriteString(", ")
	builder.WriteString("payload_json=")
	builder.WriteString(fmt.Sprintf("%v", _m.PayloadJSON))
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(_m.CreatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// ChatEntries is a parsable slice of ChatEntry.
type ChatEntries []*ChatEntry
