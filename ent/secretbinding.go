// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/BetterAndBetterII/openase/ent/secret"
	"github.com/BetterAndBetterII/openase/ent/secretbinding"
	"github.com/google/uuid"
)

// SecretBinding is the model entity for the SecretBinding schema.
type SecretBinding struct {
	config `json:"-"`
	// ID of the ent.
	ID uuid.UUID `json:"id,omitempty"`
	// OrganizationID holds the value of the "organization_id" field.
	OrganizationID uuid.UUID `json:"organization_id,omitempty"`
	// ProjectID holds the value of the "project_id" field.
	ProjectID uuid.UUID `json:"project_id,omitempty"`
	// SecretID holds the value of the "secret_id" field.
	SecretID uuid.UUID `json:"secret_id,omitempty"`
	// ScopeKind holds the value of the "scope_kind" field.
	ScopeKind secretbinding.ScopeKind `json:"scope_kind,omitempty"`
	// ScopeResourceID holds the value of the "scope_resource_id" field.
	ScopeResourceID uuid.UUID `json:"scope_resource_id,omitempty"`
	// BindingKey holds the value of the "binding_key" field.
	BindingKey string `json:"binding_key,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// UpdatedAt holds the value of the "updated_at" field.
	UpdatedAt time.Time `json:"updated_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the SecretBindingQuery when eager-loading is set.
	Edges        SecretBindingEdges `json:"edges"`
	selectValues sql.SelectValues
}

// SecretBindingEdges holds the relations/edges for other nodes in the graph.
type SecretBindingEdges struct {
	// Secret holds the value of the secret edge.
	Secret *Secret `json:"secret,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// SecretOrErr returns the Secret value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e SecretBindingEdges) SecretOrErr() (*Secret, error) {
	if e.Secret != nil {
		return e.Secret, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: secret.Label}
	}
	return nil, &NotLoadedError{edge: "secret"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*SecretBinding) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case secretbinding.FieldScopeKind, secretbinding.FieldBindingKey:
			values[i] = new(sql.NullString)
		case secretbinding.FieldCreatedAt, secretbinding.FieldUpdatedAt:
			values[i] = new(sql.NullTime)
		case secretbinding.FieldID, secretbinding.FieldOrganizationID, secretbinding.FieldProjectID, secretbinding.FieldSecretID, secretbinding.FieldScopeResourceID:
			values[i] = new(uuid.UUID)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the SecretBinding fields.
func (_m *SecretBinding) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case secretbinding.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				_m.ID = *value
			}
		case secretbinding.FieldOrganizationID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field organization_id", values[i])
			} else if value != nil {
				_m.OrganizationID = *value
			}
		case secretbinding.FieldProjectID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field project_id", values[i])
			} else if value != nil {
				_m.ProjectID = *value
			}
		case secretbinding.FieldSecretID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field secret_id", values[i])
			} else if value != nil {
				_m.SecretID = *value
			}
		case secretbinding.FieldScopeKind:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field scope_kind", values[i])
			} else if value.Valid {
				_m.ScopeKind = secretbinding.ScopeKind(value.String)
			}
		case secretbinding.FieldScopeResourceID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field scope_resource_id", values[i])
			} else if value != nil {
				_m.ScopeResourceID = *value
			}
		case secretbinding.FieldBindingKey:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field binding_key", values[i])
			} else if value.Valid {
				_m.BindingKey = value.String
			}
		case secretbinding.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				_m.CreatedAt = value.Time
			}
		case secretbinding.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				_m.UpdatedAt = value.Time
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the SecretBinding.
// This includes values selected through modifiers, order, etc.
func (_m *SecretBinding) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QuerySecret queries the "secret" edge of the SecretBinding entity.
func (_m *SecretBinding) QuerySecret() *SecretQuery {
	return NewSecretBindingClient(_m.config).QuerySecret(_m)
}

// Update returns a builder for updating this SecretBinding.
// Note that you need to call SecretBinding.Unwrap() before calling this method if this SecretBinding
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *SecretBinding) Update() *SecretBindingUpdateOne {
	return NewSecretBindingClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the SecretBinding entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *SecretBinding) Unwrap() *SecretBinding {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: SecretBinding is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *SecretBinding) String() string {
	var builder strings.Builder
	builder.WriteString("SecretBinding(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("organization_id=")
	builder.WriteString(fmt.Sprintf("%v", _m.OrganizationID))
	builder.WriteString(", ")
	builder.WriteString("project_id=")
	builder.WriteString(fmt.Sprintf("%v", _m.ProjectID))
	builder.WriteString(", ")
	builder.WriteString("secret_id=")
	builder.WriteString(fmt.Sprintf("%v", _m.SecretID))
	builder.WriteString(", ")
	builder.WriteString("scope_kind=")
	builder.WriteString(fmt.Sprintf("%v", _m.ScopeKind))
	builder.WriteString(", ")
	builder.WriteString("scope_resource_id=")
	builder.WriteString(fmt.Sprintf("%v", _m.ScopeResourceID))
	builder.WriteString(", ")
	builder.WriteString("binding_key=")
	builder.WriteString(_m.BindingKey)
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(_m.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("updated_at=")
	builder.WriteString(_m.UpdatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// SecretBindings is a parsable slice of SecretBinding.
type SecretBindings []*SecretBinding
