// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/BetterAndBetterII/openase/ent/skillblob"
	"github.com/google/uuid"
)

// SkillBlob is the model entity for the SkillBlob schema.
type SkillBlob struct {
	config `json:"-"`
	// ID of the ent.
	ID uuid.UUID `json:"id,omitempty"`
	// Sha256 holds the value of the "sha256" field.
	Sha256 string `json:"sha256,omitempty"`
	// SizeBytes holds the value of the "size_bytes" field.
	SizeBytes int64 `json:"size_bytes,omitempty"`
	// Compression holds the value of the "compression" field.
	Compression skillblob.Compression `json:"compression,omitempty"`
	// ContentBytes holds the value of the "content_bytes" field.
	ContentBytes []byte `json:"content_bytes,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the SkillBlobQuery when eager-loading is set.
	Edges        SkillBlobEdges `json:"edges"`
	selectValues sql.SelectValues
}

// SkillBlobEdges holds the relations/edges for other nodes in the graph.
type SkillBlobEdges struct {
	// Files holds the value of the files edge.
	Files []*SkillVersionFile `json:"files,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// FilesOrErr returns the Files value or an error if the edge
// was not loaded in eager-loading.
func (e SkillBlobEdges) FilesOrErr() ([]*SkillVersionFile, error) {
	if e.loadedTypes[0] {
		return e.Files, nil
	}
	return nil, &NotLoadedError{edge: "files"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*SkillBlob) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case skillblob.FieldContentBytes:
			values[i] = new([]byte)
		case skillblob.FieldSizeBytes:
			values[i] = new(sql.NullInt64)
		case skillblob.FieldSha256, skillblob.FieldCompression:
			values[i] = new(sql.NullString)
		case skillblob.FieldCreatedAt:
			values[i] = new(sql.NullTime)
		case skillblob.FieldID:
			values[i] = new(uuid.UUID)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the SkillBlob fields.
func (_m *SkillBlob) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case skillblob.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				_m.ID = *value
			}
		case skillblob.FieldSha256:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field sha256", values[i])
			} else if value.Valid {
				_m.Sha256 = value.String
			}
		case skillblob.FieldSizeBytes:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field size_bytes", values[i])
			} else if value.Valid {
				_m.SizeBytes = value.Int64
			}
		case skillblob.FieldCompression:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field compression", values[i])
			} else if value.Valid {
				_m.Compression = skillblob.Compression(value.String)
			}
		case skillblob.FieldContentBytes:
			if value, ok := values[i].(*[]byte); !ok {
				return fmt.Errorf("unexpected type %T for field content_bytes", values[i])
			} else if value != nil {
				_m.ContentBytes = *value
			}
		case skillblob.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				_m.CreatedAt = value.Time
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the SkillBlob.
// This includes values selected through modifiers, order, etc.
func (_m *SkillBlob) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryFiles queries the "files" edge of the SkillBlob entity.
func (_m *SkillBlob) QueryFiles() *SkillVersionFileQuery {
	return NewSkillBlobClient(_m.config).QueryFiles(_m)
}

// Update returns a builder for updating this SkillBlob.
// Note that you need to call SkillBlob.Unwrap() before calling this method if this SkillBlob
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *SkillBlob) Update() *SkillBlobUpdateOne {
	return NewSkillBlobClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the SkillBlob entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *SkillBlob) Unwrap() *SkillBlob {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: SkillBlob is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *SkillBlob) String() string {
	var builder strings.Builder
	builder.WriteString("SkillBlob(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("sha256=")
	builder.WriteString(_m.Sha256)
	builder.WriteString(", ")
	builder.WriteString("size_bytes=")
	builder.WriteString(fmt.Sprintf("%v", _m.SizeBytes))
	builder.WriteString(", ")
	builder.WriteString("compression=")
	builder.WriteString(fmt.Sprintf("%v", _m.Compression))
	builder.WriteString(", ")
	builder.WriteString("content_bytes=")
	builder.WriteString(fmt.Sprintf("%v", _m.ContentBytes))
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(_m.CreatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// SkillBlobs is a parsable slice of SkillBlob.
type SkillBlobs []*SkillBlob
