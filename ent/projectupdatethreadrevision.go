// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/BetterAndBetterII/openase/ent/projectupdatethread"
	"github.com/BetterAndBetterII/openase/ent/projectupdatethreadrevision"
	"github.com/google/uuid"
)

// ProjectUpdateThreadRevision is the model entity for the ProjectUpdateThreadRevision schema.
type ProjectUpdateThreadRevision struct {
	config `json:"-"`
	// ID of the ent.
	ID uuid.UUID `json:"id,omitempty"`
	// ThreadID holds the value of the "thread_id" field.
	ThreadID uuid.UUID `json:"thread_id,omitempty"`
	// RevisionNumber holds the value of the "revision_number" field.
	RevisionNumber int `json:"revision_number,omitempty"`
	// Status holds the value of the "status" field.
	Status projectupdatethreadrevision.Status `json:"status,omitempty"`
	// Title holds the value of the "title" field.
	Title string `json:"title,omitempty"`
	// BodyMarkdown holds the value of the "body_markdown" field.
	BodyMarkdown string `json:"body_markdown,omitempty"`
	// EditedBy holds the value of the "edited_by" field.
	EditedBy string `json:"edited_by,omitempty"`
	// EditedAt holds the value of the "edited_at" field.
	EditedAt time.Time `json:"edited_at,omitempty"`
	// EditReason holds the value of the "edit_reason" field.
	EditReason *string `json:"edit_reason,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the ProjectUpdateThreadRevisionQuery when eager-loading is set.
	Edges        ProjectUpdateThreadRevisionEdges `json:"edges"`
	selectValues sql.SelectValues
}

// ProjectUpdateThreadRevisionEdges holds the relations/edges for other nodes in the graph.
type ProjectUpdateThreadRevisionEdges struct {
	// Thread holds the value of the thread edge.
	Thread *ProjectUpdateThread `json:"thread,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// ThreadOrErr returns the Thread value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e ProjectUpdateThreadRevisionEdges) ThreadOrErr() (*ProjectUpdateThread, error) {
	if e.Thread != nil {
		return e.Thread, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: projectupdatethread.Label}
	}
	return nil, &NotLoadedError{edge: "thread"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*ProjectUpdateThreadRevision) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case projectupdatethreadrevision.FieldRevisionNumber:
			values[i] = new(sql.NullInt64)
		case projectupdatethreadrevision.FieldStatus, projectupdatethreadrevision.FieldTitle, projectupdatethreadrevision.FieldBodyMarkdown, projectupdatethreadrevision.FieldEditedBy, projectupdatethreadrevision.FieldEditReason:
			values[i] = new(sql.NullString)
		case projectupdatethreadrevision.FieldEditedAt:
			values[i] = new(sql.NullTime)
		case projectupdatethreadrevision.FieldID, projectupdatethreadrevision.FieldThreadID:
			values[i] = new(uuid.UUID)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the ProjectUpdateThreadRevision fields.
func (_m *ProjectUpdateThreadRevision) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case projectupdatethreadrevision.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				_m.ID = *value
			}
		case projectupdatethreadrevision.FieldThreadID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field thread_id", values[i])
			} else if value != nil {
				_m.ThreadID = *value
			}
		case projectupdatethreadrevision.FieldRevisionNumber:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field revision_number", values[i])
			} else if value.Valid {
				_m.RevisionNumber = int(value.Int64)
			}
		case projectupdatethreadrevision.FieldStatus:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field status", values[i])
			} else if value.Valid {
				_m.Status = projectupdatethreadrevision.Status(value.String)
			}
		case projectupdatethreadrevision.FieldTitle:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field title", values[i])
			} else if value.Valid {
				_m.Title = value.String
			}
		case projectupdatethreadrevision.FieldBodyMarkdown:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field body_markdown", values[i])
			} else if value.Valid {
				_m.BodyMarkdown = value.String
			}
		case projectupdatethreadrevision.FieldEditedBy:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field edited_by", values[i])
			} else if value.Valid {
				_m.EditedBy = value.String
			}
		case projectupdatethreadrevision.FieldEditedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field edited_at", values[i])
			} else if value.Valid {
				_m.EditedAt = value.Time
			}
		case projectupdatethreadrevision.FieldEditReason:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field edit_reason", values[i])
			} else if value.Valid {
				_m.EditReason = new(string)
				*_m.EditReason = value.String
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the ProjectUpdateThreadRevision.
// This includes values selected through modifiers, order, etc.
func (_m *ProjectUpdateThreadRevision) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryThread queries the "thread" edge of the ProjectUpdateThreadRevision entity.
func (_m *ProjectUpdateThreadRevision) QueryThread() *ProjectUpdateThreadQuery {
	return NewProjectUpdateThreadRevisionClient(_m.config).QueryThread(_m)
}

// Update returns a builder for updating this ProjectUpdateThreadRevision.
// Note that you need to call ProjectUpdateThreadRevision.Unwrap() before calling this method if this ProjectUpdateThreadRevision
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *ProjectUpdateThreadRevision) Update() *ProjectUpdateThreadRevisionUpdateOne {
	return NewProjectUpdateThreadRevisionClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the ProjectUpdateThreadRevision entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *ProjectUpdateThreadRevision) Unwrap() *ProjectUpdateThreadRevision {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: ProjectUpdateThreadRevision is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *ProjectUpdateThreadRevision) String() string {
	var builder strings.Builder
	builder.WriteString("ProjectUpdateThreadRevision(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("thread_id=")
	builder.WriteString(fmt.Sprintf("%v", _m.ThreadID))
	builder.WriteString(", ")
	builder.WriteString("revision_number=")
	builder.WriteString(fmt.Sprintf("%v", _m.RevisionNumber))
	builder.WriteString(", ")
	builder.WriteString("status=")
	builder.WriteString(fmt.Sprintf("%v", _m.Status))
	builder.WriteString(", ")
	builder.WriteString("title=")
	builder.WriteString(_m.Title)
	builder.WriteString(", ")
	builder.WriteString("body_markdown=")
	builder.WriteString(_m.BodyMarkdown)
	builder.WriteString(", ")
	builder.WriteString("edited_by=")
	builder.WriteString(_m.EditedBy)
	builder.WriteString(", ")
	builder.WriteString("edited_at=")
	builder.WriteString(_m.EditedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	if v := _m.EditReason; v != nil {
		builder.WriteString("edit_reason=")
		builder.WriteString(*v)
	}
	builder.WriteByte(')')
	return builder.String()
}

// ProjectUpdateThreadRevisions is a parsable slice of ProjectUpdateThreadRevision.
type ProjectUpdateThreadRevisions []*ProjectUpdateThreadRevision
