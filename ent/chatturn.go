// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/BetterAndBetterII/openase/ent/chatconversation"
	"github.com/BetterAndBetterII/openase/ent/chatturn"
	"github.com/google/uuid"
)

// ChatTurn is the model entity for the ChatTurn schema.
type ChatTurn struct {
	config `json:"-"`
	// ID of the ent.
	ID uuid.UUID `json:"id,omitempty"`
	// ConversationID holds the value of the "conversation_id" field.
	ConversationID uuid.UUID `json:"conversation_id,omitempty"`
	// TurnIndex holds the value of the "turn_index" field.
	TurnIndex int `json:"turn_index,omitempty"`
	// ProviderTurnID holds the value of the "provider_turn_id" field.
	ProviderTurnID *string `json:"provider_turn_id,omitempty"`
	// Status holds the value of the "status" field.
	Status string `json:"status,omitempty"`
	// StartedAt holds the value of the "started_at" field.
	StartedAt time.Time `json:"started_at,omitempty"`
	// CompletedAt holds the value of the "completed_at" field.
	CompletedAt *time.Time `json:"completed_at,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// UpdatedAt holds the value of the "updated_at" field.
	UpdatedAt time.Time `json:"updated_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the ChatTurnQuery when eager-loading is set.
	Edges        ChatTurnEdges `json:"edges"`
	selectValues sql.SelectValues
}

// ChatTurnEdges holds the relations/edges for other nodes in the graph.
type ChatTurnEdges struct {
	// Conversation holds the value of the conversation edge.
	Conversation *ChatConversation `json:"conversation,omitempty"`
	// Entries holds the value of the entries edge.
	Entries []*ChatEntry `json:"entries,omitempty"`
	// PendingInterrupts holds the value of the pending_interrupts edge.
	PendingInterrupts []*ChatPendingInterrupt `json:"pending_interrupts,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [3]bool
}

// ConversationOrErr returns the Conversation value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e ChatTurnEdges) ConversationOrErr() (*ChatConversation, error) {
	if e.Conversation != nil {
		return e.Conversation, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: chatconversation.Label}
	}
	return nil, &NotLoadedError{edge: "conversation"}
}

// EntriesOrErr returns the Entries value or an error if the edge
// was not loaded in eager-loading.
func (e ChatTurnEdges) EntriesOrErr() ([]*ChatEntry, error) {
	if e.loadedTypes[1] {
		return e.Entries, nil
	}
	return nil, &NotLoadedError{edge: "entries"}
}

// PendingInterruptsOrErr returns the PendingInterrupts value or an error if the edge
// was not loaded in eager-loading.
func (e ChatTurnEdges) PendingInterruptsOrErr() ([]*ChatPendingInterrupt, error) {
	if e.loadedTypes[2] {
		return e.PendingInterrupts, nil
	}
	return nil, &NotLoadedError{edge: "pending_interrupts"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*ChatTurn) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case chatturn.FieldTurnIndex:
			values[i] = new(sql.NullInt64)
		case chatturn.FieldProviderTurnID, chatturn.FieldStatus:
			values[i] = new(sql.NullString)
		case chatturn.FieldStartedAt, chatturn.FieldCompletedAt, chatturn.FieldCreatedAt, chatturn.FieldUpdatedAt:
			values[i] = new(sql.NullTime)
		case chatturn.FieldID, chatturn.FieldConversationID:
			values[i] = new(uuid.UUID)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the ChatTurn fields.
func (_m *ChatTurn) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case chatturn.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				_m.ID = *value
			}
		case chatturn.FieldConversationID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field conversation_id", values[i])
			} else if value != nil {
				_m.ConversationID = *value
			}
		case chatturn.FieldTurnIndex:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field turn_index", values[i])
			} else if value.Valid {
				_m.TurnIndex = int(value.Int64)
			}
		case chatturn.FieldProviderTurnID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field provider_turn_id", values[i])
			} else if value.Valid {
				_m.ProviderTurnID = new(string)
				*_m.ProviderTurnID = value.String
			}
		case chatturn.FieldStatus:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field status", values[i])
			} else if value.Valid {
				_m.Status = value.String
			}
		case chatturn.FieldStartedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field started_at", values[i])
			} else if value.Valid {
				_m.StartedAt = value.Time
			}
		case chatturn.FieldCompletedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field completed_at", values[i])
			} else if value.Valid {
				_m.CompletedAt = new(time.Time)
				*_m.CompletedAt = value.Time
			}
		case chatturn.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				_m.CreatedAt = value.Time
			}
		case chatturn.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				_m.UpdatedAt = value.Time
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the ChatTurn.
// This includes values selected through modifiers, order, etc.
func (_m *ChatTurn) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryConversation queries the "conversation" edge of the ChatTurn entity.
func (_m *ChatTurn) QueryConversation() *ChatConversationQuery {
	return NewChatTurnClient(_m.config).QueryConversation(_m)
}

// QueryEntries queries the "entries" edge of the ChatTurn entity.
func (_m *ChatTurn) QueryEntries() *ChatEntryQuery {
	return NewChatTurnClient(_m.config).QueryEntries(_m)
}

// QueryPendingInterrupts queries the "pending_interrupts" edge of the ChatTurn entity.
func (_m *ChatTurn) QueryPendingInterrupts() *ChatPendingInterruptQuery {
	return NewChatTurnClient(_m.config).QueryPendingInterrupts(_m)
}

// Update returns a builder for updating this ChatTurn.
// Note that you need to call ChatTurn.Unwrap() before calling this method if this ChatTurn
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *ChatTurn) Update() *ChatTurnUpdateOne {
	return NewChatTurnClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the ChatTurn entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *ChatTurn) Unwrap() *ChatTurn {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: ChatTurn is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *ChatTurn) String() string {
	var builder strings.Builder
	builder.WriteString("ChatTurn(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("conversation_id=")
	builder.WriteString(fmt.Sprintf("%v", _m.ConversationID))
	builder.WriteString(", ")
	builder.WriteString("turn_index=")
	builder.WriteString(fmt.Sprintf("%v", _m.TurnIndex))
	builder.WriteString(", ")
	if v := _m.ProviderTurnID; v != nil {
		builder.WriteString("provider_turn_id=")
		builder.WriteString(*v)
	}
	builder.WriteString(", ")
	builder.WriteString("status=")
	builder.WriteString(_m.Status)
	builder.WriteString(", ")
	builder.WriteString("started_at=")
	builder.WriteString(_m.StartedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	if v := _m.CompletedAt; v != nil {
		builder.WriteString("completed_at=")
		builder.WriteString(v.Format(time.ANSIC))
	}
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(_m.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("updated_at=")
	builder.WriteString(_m.UpdatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// ChatTurns is a parsable slice of ChatTurn.
type ChatTurns []*ChatTurn
